/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allows application to access the links with the below hotnames
  images: {
    domains: ["lh3.googleusercontent.com", "images.unsplash.com"],
  },
};

module.exports = nextConfig;
